/**
 * sudoku9x9+4.js
 * Sudoku (with extra constraint squares) DLX wrapper
 */

function sudoku(matrix9x9) {
    // Using the pre-filled matrix, build the
    // constraints in jumping links 0,1-matrix:
    // column for each constraint: square, row, column (extra 2x2 x 3x3center squares)
    // pre-hide some columns based on already filled numbers? or what?

    // Call search(headers, solution, 0) // choose, callback

    // Convert the solution back to the matrix9x9 form

    return matrix9x9;
}